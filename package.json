{
  "name": "sensory-radio",
  "version": "1.0.0",
  "private": true,
  "description": "Static radio player + wall dashboard served by a Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
